import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['github.com', 'encrypted-tbn0.gstatic.com', 'external-content.duckduckgo.com']
  }
};

export default nextConfig;
